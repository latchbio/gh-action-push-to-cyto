{
  "private": true,
  "scripts": {
    "build": "yarn run tsc --project src/tsconfig.json",
    "package": "yarn run ncc build src/index.ts --transpile-only --minify"
  },
  "dependencies": {
    "@actions/artifact": "v2",
    "@actions/core": "^1.9.0",
    "@actions/exec": "^1.1.1",
    "@actions/tool-cache": "^2.0.1"
  },
  "devDependencies": {
    "@types/node": "^17.0.44",
    "@vercel/ncc": "^0.34.0",
    "typescript": "^4.7.3"
  }
}
